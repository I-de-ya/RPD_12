# coding: utf-8
-if current_user.admin?
  %h1.title Новый доклад
-else
  = (link_to "<< Back, to applications list", speeches_path) if current_user.speeches.count != 0
  %p= t(:attention_application_deadline) + application_deadline + "."
%p
  .rpd_form
    %fieldset
      %legend= t(:fill_in_form)
      = render 'form'
-if current_user.admin?
  = link_to "<< Назад, к докладам", speeches_path