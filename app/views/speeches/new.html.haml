# coding: utf-8
-if current_user.admin?
	%h1.title Новый доклад
-else
	= "Внимание! Заявки принимаются до #{speech_deadline}."
%p
	.rpd_form
		%fieldset
			%legend Заполните заявку
			= render 'form'
-if current_user.admin?
	= link_to "<< Назад, к докладам", speeches_path
