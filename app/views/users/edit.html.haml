%h3= "Текущий пользователь#{" не" unless @user.admin?} является администратором"
= form_for @user do |f|
	= f.submit (@user.admin? ? "Убрать права администратора" : "Добавить права администратора")
%p= link_to "Назад", user_path